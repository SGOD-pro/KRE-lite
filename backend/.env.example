# .env.example — copy to .env and fill in real values.
# Do NOT commit .env to source control (it's in .gitignore).
#
# ─────────────────────────────────────────────────────────────────
# ENV controls the boto3 credential/endpoint strategy:
#
#   dev   → profile_name="aws"   | real AWS (Bedrock + RDS + ElastiCache)
#   local → profile_name="local" | LocalStack at http://localhost:4566
#             RDS + Redis still connect to localhost via psycopg2/redis-py
#   prod  → no profile           | IAM role (ECS / EC2 / Lambda)
# ─────────────────────────────────────────────────────────────────
ENV=dev

# AWS region
AWS_REGION=ap-south-1

# LocalStack endpoint (only read when ENV=local)
LOCALSTACK_ENDPOINT=http://localhost:4566

# ── Bedrock model IDs ─────────────────────────────────────────────
TITAN_EMBED_MODEL_ID=amazon.titan-embed-text-v2:0
NOVA_LLM_MODEL_ID=amazon.nova-pro-v1:0

# ── RDS (PostgreSQL + pgvector) ───────────────────────────────────
# dev/prod → replace with your real RDS endpoint
# local    → localhost (postgres running locally or in Docker)
RDS_HOST=cited-or-silent-db.c3kqywkyq8ar.ap-south-1.rds.amazonaws.com
RDS_PORT=5432
RDS_DB=postgres
RDS_USER=postgres
RDS_PASSWORD=changeme

# ── ElastiCache (Redis) ───────────────────────────────────────────
# dev/prod → replace with your real ElastiCache endpoint
# local    → localhost (Redis running locally or in Docker)
REDIS_HOST=<your-elasticache-endpoint>
REDIS_PORT=6379
