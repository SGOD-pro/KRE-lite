# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ── Validation & Config ───────────────────────────────────────────────────────
pydantic>=2.7.0
python-dotenv>=1.0.0

# ── File upload support ───────────────────────────────────────────────────────
python-multipart>=0.0.9

# ── PDF Processing ────────────────────────────────────────────────────────────
pymupdf>=1.24.0

# ── Embeddings: BGE-small-en-v1.5 ONNX (local, CPU, no API call) ─────────────
# onnxruntime runs the ONNX model; sentence-transformers provides the tokenizer
onnxruntime>=1.18.0
sentence-transformers>=3.0.0

# ── BM25 retrieval ────────────────────────────────────────────────────────────
rank-bm25>=0.2.2

# ── Vector store: Chroma embedded mode (local, no Docker service needed) ──────
chromadb>=0.5.0

# ── Fuzzy match for citation_verifier (Phase 2) ───────────────────────────────
thefuzz>=0.22.1
python-Levenshtein>=0.25.0  # speeds up thefuzz

# ── LLM call — OpenAI-compatible client for NVIDIA Build / OpenRouter (Phase 2)
openai>=1.35.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
httpx2>=0.28.0