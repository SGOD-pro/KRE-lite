fastapi
uvicorn[standard]
pytest
pytest-asyncio
httpx
python-multipart

# PDF parsing
pymupdf

# AWS Bedrock (Titan Embeddings + Nova LLM)
boto3

# MongoDB Atlas
pymongo

# Qdrant Cloud
qdrant-client

# Fuzzy matching for citation verifier (Phase 2)
rapidfuzz

# Numpy for cosine similarity in vector_retriever
numpy

# .env loading
python-dotenv
