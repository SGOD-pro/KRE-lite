# ── Web Framework ─────────────────────────────────────────────────────────────
# Note: pinned to versions that have pre-built wheels for Python 3.13
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ── Validation & Config ───────────────────────────────────────────────────────
pydantic>=2.7.0
python-dotenv>=1.0.0

# ── File upload support ───────────────────────────────────────────────────────
python-multipart>=0.0.9

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
httpx2>=0.28.0

# ── Phase 1+ deps (to be added when Phase 1 starts) ──────────────────────────
# pymupdf         — PDF chunker
# rank-bm25       — BM25 retrieval
# chromadb        — vector store (local, no external service)
# onnxruntime     — BGE-small-en-v1.5 ONNX embeddings
# sentence-transformers — tokenizer helper for BGE-small
# openai          — LLM call (OpenRouter / NVIDIA Build via openai-compat)
# thefuzz         — fuzzy-match in citation_verifier